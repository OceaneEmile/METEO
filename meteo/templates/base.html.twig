<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Welcome!</title>

    <!-- Balises pour le responsive -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">

    <!-- Notre CSS custom -->
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">
 
</head>
    <body class="mb-5">
    <nav class="container py-3 mb-3">
        <ul class="nav nav-pills">
            <li class="nav-item">
                <a class="nav-link active" href="{{ path('main_home') }}">Accueil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link " href="{{ path('app_mountain') }}">Montagne</a>
            </li>
            <li class="nav-item">
                <a class="nav-link " href="{{ path('app_beaches') }}">Plages</a>
            </li>
        </ul>
    </nav>
            {% block main %}
            {# Ici on va traiter le contenu de <body> dans toutes les vue qui étende base.html.twig #}
        {% endblock %}

    </body>
</html>
